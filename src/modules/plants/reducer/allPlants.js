import {} from "../constants";

const initialAllPlantsState = [1, 2];

const allPlants = (state = initialAllPlantsState, { type, data }) => {
  switch (type) {
    default:
      return state;
  }
};

export default allPlants;
